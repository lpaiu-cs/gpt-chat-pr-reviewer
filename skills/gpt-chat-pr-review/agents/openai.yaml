interface:
  display_name: "GPT Chat PR Review"
  short_description: "Request and manage ChatGPT-based PR reviews"
  default_prompt: "Use $gpt-chat-pr-review to request a review for this GitHub PR and handle the result through convergence."
policy:
  allow_implicit_invocation: false
